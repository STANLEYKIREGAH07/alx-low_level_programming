#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
char c;
int d;
for (d = '0'; d <= '9'; d++)
	putchar(0);
for (c = 'a'; c <= 'f'; c++)
	putchar(c);
putchar('\n');
return (0);
}

